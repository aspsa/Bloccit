# Checkpoint #55 - Favoriting
#
# Create a policy file for favorites. The ApplicationPolicy defaults will work for favorites, so the file can be empty apart from the class definition (and inheritance declaration).
class FavoritePolicy < ApplicationPolicy
    
end