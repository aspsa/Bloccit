<h1>Edit Question</h1>

<div class="row">
    <div class="col-md-4">
        <p>Guidelines for questions</p>
        <ul>
            <li>Make sure the question is relevant to the topic at hand.</li>
            <li>Keep the quesiton focused. If you have several questions about a topic, create them separately.</li>
        </ul>
    </div>
    <div class="col-md-8">
        <%= form_for @question do |f| %>
            <div class="form-group">
                <%= f.label :title %>
                <%= f.text_field :title, class: 'form-control', placeholder: "Question Topic" %>
            </div>
            <div class="form-group">
                <%= f.label :body %>
                <%= f.text_area :body, class: 'form-control', placeholder: "The Actual Question" %>
            </div>
            <div class="form-group">
                <%= f.label "Resolved?" %>
                <%= f.check_box "resolved" %>
            </div>
            <div class="form-group">
                <%= f.submit "Save", class: 'btn btn-success' %>
            </div>
        <% end %>
    </div>
</div>